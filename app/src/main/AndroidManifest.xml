<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.com.victoriasantos.libertadoresupdates">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".view.activities.jogos.JogosAcontecendoActivity"></activity>
        <activity android:name=".view.activities.jogos.JogosAnterioresActivity" />
        <activity android:name=".view.activities.jogos.ProximosJogosActivity" />
        <activity android:name=".view.activities.jogos.JogosActivity" />
        <activity android:name=".view.activities.TimesActivity" />
        <activity android:name=".view.activities.GruposActivity" />
        <activity android:name=".view.activities.ProfileActivity" />
        <activity android:name=".view.activities.cadastro_login.RecuperacaoSenhaActivity" />
        <activity android:name=".view.activities.cadastro_login.CadastroActivity" />
        <activity android:name=".view.activities.cadastro_login.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".view.activities.MainActivity" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>